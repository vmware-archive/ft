platform: linux
image_resource:
  type: registry-image
  source:
    repository: vito/oci-build-task
inputs:
- name: repo
  path: .
- name: postgresql-11-bionic
outputs:
- name: built-image
run:
  path: build
